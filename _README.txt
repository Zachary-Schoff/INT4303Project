All of the files will work as is once the database is setup, if all of the files are in the same directory,
and the image files have their own subdirectory named 'image', and given that you have ran the 'Database.sql'
on your MySQL database (this database schema must be named 'enhancedfunctionalitypages')